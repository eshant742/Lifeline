<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="8dp"
    android:layout_marginVertical="6dp"
    app:cardBackgroundColor="@color/dark_surface"
    app:cardCornerRadius="16dp"
    app:strokeWidth="1dp"
    app:strokeColor="@color/border_color"
    app:cardElevation="0dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <!-- Status Indicator Bar -->
        <View
            android:id="@+id/statusIndicator"
            android:layout_width="4dp"
            android:layout_height="match_parent"
            android:background="@color/emergency_red"/>

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical"
            android:padding="16dp">

            <!-- Header Row: Name + Priority Badge + Status -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical">

                <!-- Message Type Icon -->
                <TextView
                    android:id="@+id/tvMessageTypeIcon"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="🆘"
                    android:textSize="16sp"
                    android:layout_marginEnd="6dp"/>

                <TextView
                    android:id="@+id/tvSenderName"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:text="Sender Name"
                    android:textSize="16sp"
                    android:textStyle="bold"
                    android:textColor="@color/text_primary"/>

                <!-- Priority Badge -->
                <TextView
                    android:id="@+id/tvPriority"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="P5"
                    android:textSize="10sp"
                    android:textStyle="bold"
                    android:textColor="@color/white"
                    android:paddingHorizontal="6dp"
                    android:paddingVertical="2dp"
                    android:layout_marginEnd="6dp"
                    android:background="@color/emergency_red"/>

                <TextView
                    android:id="@+id/tvStatus"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="TRAPPED"
                    android:textSize="11sp"
                    android:textStyle="bold"
                    android:textColor="@color/white"
                    android:paddingHorizontal="8dp"
                    android:paddingVertical="4dp"
                    android:background="@color/emergency_red"/>
            </LinearLayout>

            <!-- Phone + Role -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical"
                android:layout_marginTop="2dp">

                <TextView
                    android:id="@+id/tvSenderPhone"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:text="📞 Phone"
                    android:textSize="13sp"
                    android:textColor="@color/text_secondary"/>

                <!-- Hop Count / Relay Indicator -->
                <TextView
                    android:id="@+id/tvHopCount"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="↗ Direct"
                    android:textSize="11sp"
                    android:textColor="@color/text_secondary"/>
            </LinearLayout>

            <!-- Content -->
            <TextView
                android:id="@+id/tvContent"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="SOS Message"
                android:textSize="14sp"
                android:textColor="@color/text_primary"
                android:layout_marginTop="12dp"
                android:lineSpacingExtra="4dp"/>

            <!-- Medical Info (only shown for SOS with medical data) -->
            <LinearLayout
                android:id="@+id/layoutMedicalInfo"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:visibility="gone"
                android:layout_marginTop="8dp"
                android:padding="8dp"
                android:background="@color/dark_surface_variant">

                <TextView
                    android:id="@+id/tvMedicalInfo"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="🩸 O+ | ⚠️ Penicillin"
                    android:textSize="12sp"
                    android:textColor="@color/warning_amber"
                    android:textStyle="bold"/>
            </LinearLayout>

            <!-- Rescue Status (only shown when rescue is in progress) -->
            <TextView
                android:id="@+id/tvRescueStatus"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="🚑 Rescuer: John — EN_ROUTE"
                android:textSize="12sp"
                android:textColor="@color/safe_green"
                android:textStyle="bold"
                android:visibility="gone"
                android:layout_marginTop="8dp"/>

            <!-- Footer Row -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical"
                android:layout_marginTop="16dp">

                <TextView
                    android:id="@+id/tvTimestamp"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:text="Timestamp"
                    android:textSize="12sp"
                    android:textColor="@color/text_secondary"/>

                <com.google.android.material.card.MaterialCardView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    app:cardBackgroundColor="@color/dark_surface_variant"
                    app:cardCornerRadius="8dp"
                    app:strokeWidth="1dp"
                    app:strokeColor="@color/border_color"
                    app:cardElevation="0dp">

                    <TextView
                        android:id="@+id/tvLocation"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="📍 0.0, 0.0"
                        android:textSize="11sp"
                        android:textColor="@color/text_secondary"
                        android:paddingHorizontal="8dp"
                        android:paddingVertical="4dp"/>
                </com.google.android.material.card.MaterialCardView>
            </LinearLayout>
        </LinearLayout>
    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
